import React from 'react'
import Typography from '@material-ui/core/Typography'

const Loading = (props) => {
  return (
    <Typography>
      Loading...
    </Typography>
  )
}

export default Loading